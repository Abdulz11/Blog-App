// // 'use client'


// export default async function ShowLoginButton(){
//     // const session = await auth()

//     // return ( <h1>Welcome {session ? session?.user?.name : null }</h1>)
//     return ( )

// }