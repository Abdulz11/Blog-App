
import { getAllByRole, getByDisplayValue, getByText, render, screen } from "@testing-library/react"
import Navbar from "@/components/Navbar/navbar"


describe('Navbar',()=>{
    it('navbar is present',()=>{
        // render(<Navbar/>)
        // const links = screen.getAllByRole('a',{name:'link'})
        // console.log(links)
        // expect(links).toBeInTheDocument()
    })
    // it('navbar does not show private links if not logged in')
    // render(<Navbar/>)
    // const loginButton = getByText('Login');
    // const menuBtn = getByText('menu')
    // const  loginButton = getByText('profile');
    // expect(loginButton).toHaveDisplayValue('Login');
    // await userEvent.click(menuButton)
    
    
})
